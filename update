#!/usr/bin/env bash

# Strict mode for safety
set -euo pipefail

# === Helpers ===

# Check for sudo availability and usage
function run_priv() {
    if [ "$EUID" -ne 0 ]; then
        if command -v sudo &>/dev/null; then
            sudo "$@"
        else
            echo "❌ Error: This command requires root privileges, but sudo is not installed."
            return 1
        fi
    else
        "$@"
    fi
}

# === Core Functions ===

function update_system() {
    echo -e "\n🔄 Detecting OS and updating system packages..."

    if [ -f /etc/os-release ]; then
        # Source the OS release file to get ID and ID_LIKE
        . /etc/os-release

        # Normalize distros based on ID or ID_LIKE
        case "${ID:-linux}" in
            debian|ubuntu|linuxmint|pop|kali|raspbian)
                echo "👉 Detected Debian/Ubuntu based system."
                run_priv apt-get update && run_priv apt-get upgrade -y && run_priv apt-get autoremove -y
                ;;
            fedora|rhel|centos|almalinux|rocky)
                echo "👉 Detected RHEL/Fedora based system."
                run_priv dnf upgrade --refresh -y
                ;;
            arch|manjaro|endeavouros|garuda)
                echo "👉 Detected Arch Linux based system."

                # Check if running as root (AUR helpers generally discourage/block root)
                if [ "$EUID" -eq 0 ]; then
                    echo "⚠️  Running as root: Skipping AUR helpers and using pacman directly."
                    pacman -Syu --noconfirm
                else
                    # Check for Common AUR Helpers
                    if command -v paru &>/dev/null; then
                        echo "✨ Found AUR helper: paru"
                        # paru updates both repo and AUR
                        paru -Syu
                    elif command -v yay &>/dev/null; then
                        echo "✨ Found AUR helper: yay"
                        yay -Syu
                    elif command -v pikaur &>/dev/null; then
                        echo "✨ Found AUR helper: pikaur"
                        pikaur -Syu
                    else
                        echo "ℹ️  No AUR helper found. Using standard pacman."
                        run_priv pacman -Syu --noconfirm
                    fi
                fi
                ;;
            opensuse*|suse)
                echo "👉 Detected OpenSUSE."
                run_priv zypper refresh && run_priv zypper update -y
                ;;
            alpine)
                echo "👉 Detected Alpine Linux."
                run_priv apk update && run_priv apk upgrade
                ;;
            void)
                echo "👉 Detected Void Linux."
                run_priv xbps-install -Su
                ;;
            *)
                # Fallback Logic
                if [[ "${ID_LIKE:-}" == *"arch"* ]]; then
                    # Repeat Arch Logic for ID_LIKE matches
                    if [ "$EUID" -ne 0 ] && command -v yay &>/dev/null; then
                        echo "✨ Found AUR helper: yay"
                        yay -Syu
                    elif [ "$EUID" -ne 0 ] && command -v paru &>/dev/null; then
                        echo "✨ Found AUR helper: paru"
                        paru -Syu
                    else
                        run_priv pacman -Syu --noconfirm
                    fi
                elif [[ "${ID_LIKE:-}" == *"debian"* ]]; then
                    run_priv apt-get update && run_priv apt-get upgrade -y
                elif [[ "${ID_LIKE:-}" == *"rhel"* || "${ID_LIKE:-}" == *"fedora"* ]]; then
                    run_priv dnf upgrade --refresh -y
                else
                    echo "⚠️  Could not detect package manager for '$ID'. Skipping system update."
                fi
                ;;
        esac
    else
        echo "⚠️  /etc/os-release not found. Cannot determine OS."
    fi
}

function update_flatpak() {
    if command -v flatpak &>/dev/null; then
        echo -e "\n📦 Updating Flatpak..."
        flatpak update -y
    fi
}

function update_rustup() {
    if command -v rustup &>/dev/null; then
        echo -e "\n🦀 Updating Rustup..."
        rustup update
    fi
}

function update_bob() {
    if command -v bob &>/dev/null; then
        echo -e "\n🎛️ Updating Bob (Neovim Version Manager)..."
        bob update
    fi
}

function update_pipx() {
    if command -v pipx &>/dev/null; then
        echo -e "\n🐍 Updating pipx..."
        pipx upgrade-all
    fi
}

function update_neovim() {
    if command -v nvim &>/dev/null; then
        echo -e "\n💡 Updating Neovim plugins..."
        nvim --headless "+Lazy! sync" +qa
    fi
}

function update_tmux_plugins() {
    TPM="${XDG_CONFIG_HOME:-$HOME/.config}/tmux/plugins/tpm/bin/update_plugins"
    if [ ! -f "$TPM" ]; then
        TPM="$HOME/.tmux/plugins/tpm/bin/update_plugins"
    fi

    if [ -x "$TPM" ]; then
        echo -e "\n🔧 Updating tmux plugins..."
        "$TPM" all
    else
        echo "ℹ️  TPM script not found. Skipping."
    fi
}

function reload_tmux_config() {
    if [ -n "${TMUX:-}" ]; then
        local tmux_conf="${XDG_CONFIG_HOME:-$HOME/.config}/tmux/tmux.conf"
        [ ! -f "$tmux_conf" ] && tmux_conf="$HOME/.tmux.conf"

        if [ -f "$tmux_conf" ]; then
            tmux source-file "$tmux_conf"
            echo "♻️  Reloaded tmux config"
        fi
    else
        echo "ℹ️  Not inside a tmux session."
    fi
}

function run_all() {
    update_system
    update_flatpak
    update_rustup
    update_bob
    update_pipx
    update_neovim
    update_tmux_plugins
    reload_tmux_config
}

# === Menus ===

function fzf_menu() {
    echo -e "🔄 System Update\n📦 Flatpak Update\n🦀 Rustup Update\n🎛️ Bob Update\n🐍 pipx Update\n💡 Neovim Plugin Update\n🔧 Tmux Plugin Update\n♻️ Reload Tmux Config\n✅ Run All\n❌ Exit" | fzf --prompt="Dashboard > "
}

function simple_menu() {
    echo -e "\n--- Update Dashboard ---"
    PS3="Select an option: "
    options=("System Update" "Flatpak Update" "Rustup Update" "Bob Update" "pipx Update" "Neovim Plugin Update" "Tmux Plugin Update" "Reload Tmux Config" "Run All" "Exit")
    select opt in "${options[@]}"; do
        case $opt in
            "System Update") update_system; return 0 ;;
            "Flatpak Update") update_flatpak; return 0 ;;
            "Rustup Update") update_rustup; return 0 ;;
            "Bob Update") update_bob; return 0 ;;
            "pipx Update") update_pipx; return 0 ;;
            "Neovim Plugin Update") update_neovim; return 0 ;;
            "Tmux Plugin Update") update_tmux_plugins; return 0 ;;
            "Reload Tmux Config") reload_tmux_config; return 0 ;;
            "Run All") run_all; return 0 ;;
            "Exit") echo "❌ Exit"; return 0 ;;
            *) echo "Invalid option $REPLY" ;;
        esac
    done
}

# === Run Loop ===

while true; do
    if command -v fzf &>/dev/null; then
        SELECTION=$(fzf_menu)
    else
        SELECTION=$(simple_menu)
    fi

    if [ -z "$SELECTION" ]; then break; fi

    case "$SELECTION" in
        *"System Update") update_system ;;
        *"Flatpak Update") update_flatpak ;;
        *"Rustup Update") update_rustup ;;
        *"Bob Update") update_bob ;;
        *"pipx Update") update_pipx ;;
        *"Neovim Plugin Update") update_neovim ;;
        *"Tmux Plugin Update") update_tmux_plugins ;;
        *"Reload Tmux Config") reload_tmux_config ;;
        *"Run All") run_all ;;
        *"Exit") break ;;
    esac

    echo -e "\n✅ Done. Press Enter to return to the dashboard..."
    read -r
done
